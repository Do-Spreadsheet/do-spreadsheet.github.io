name: generateData

on:
  # Triggers the workflow on push events but only for the master branch
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

steps:
  - name: run generateData.js
    run: |
      node generateData.js
